# Scripts

- `seed.ts` — базовое наполнение данных.
- `seed-org-and-users.ts` — генерация организаций и учетных записей.

`read-excel.js` удалён, так как использовал отсутствующий Excel-файл из `attached_assets`.
